# Copy to .env.local for local dev (.env.local is gitignored — never commit real keys).
# On Render/Railway, set ANTHROPIC_API_KEY in the host's Environment settings instead.

# Powers the Treasury IQ Assistant (chat) and the quarterly Refresh extraction.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
